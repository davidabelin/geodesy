Werkzeug>=2.0.3  # Ensure compatibility with Flask
Flask>=2.3.2
gunicorn>=20.1.0
protobuf
pyyaml
black>=23.9.1
flake8>=6.0.0
mypy>=1.5.1
pre-commit>=3.4.0
pytest
pyproj
geopandas
pandas
pyogrio
rasterio>=1.5.0
shapely
matplotlib>=3.10.8
scipy>=1.17.0
numpy>=2.4.0
networkx>=3.0.0
geojson>=2.5.0
# QGIS Desktop Python work in this repo uses the OSGeo4W runtime on Windows,
# not a pip-installed qgis/osgeo/gdal stack. Normal Python raster/vector work
# should use rasterio, pyogrio, geopandas, pyproj, and shapely. The Hawkins
# workflow is targeted at the QGIS 4.x runtime exposed by OSGeo4W's
# python-qgis.bat launcher.
